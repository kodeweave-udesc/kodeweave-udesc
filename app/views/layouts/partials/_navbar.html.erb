<nav class="navbar navbar-expand navbar-dark bg-dark static-top">
  <button class="btn btn-link btn-sm text-white order-1 order-sm-0" id="sidebarToggle" href="#">
    <%= icon('fas','bars', class: 'h5 mb-0') %>
  </button>
  <%= link_to('Kode Weave', root_path, class:"navbar-brand mr-1") %>

  <!-- Navbar Search -->
  <div class="ml-auto" />

  <!-- Navbar -->
  <ul class="navbar-nav ml-auto ml-md-0">
    <li class="nav-item dropdown no-arrow">
      <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="margin:0;padding:0;">
        <%= current_user.name %>
        <button class="btn btn-link btn-sm text-white order-1 order-sm-0 pb-0 pt-0">
          <%= image_tag(current_user.image, class: "avatar-image") %>
        </button>
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <%= link_to(t('.settings'), edit_user_registration_path, class: 'dropdown-item') %>
        <%= link_to(t('.log_list'), '#', class: 'dropdown-item') %>
        <div class="dropdown-divider"></div>
        <%= link_to(t('.log_out'), destroy_user_session_path, method: :delete, class: 'dropdown-item') %>
      </div>
    </li>
  </ul>
</nav>

<style>
.avatar-image {
  height: 42px;
  width: 42px;
  padding: 0px;
  margin: 0px;
  border: 2px solid;
  border-radius: 25px;
}
</style>
