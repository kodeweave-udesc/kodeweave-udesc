<div class="card mb-3 bg-light ">
  <div class="card-header h3">
    <%= icon('fas','user-astronaut', t('.title'), class: 'fa-fw h2 mb-0') %>
    <%= link_to(icon('fas', 'plus', t('.new')), new_account_user_path, class: "btn btn-success float-right") %>
  </div>
  <div class="card-body">
    <div class="row">
      <%= render(@users) %>
    </div>
  </div>
</div>
