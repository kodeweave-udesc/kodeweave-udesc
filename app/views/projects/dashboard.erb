<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','desktop', t('.title', project_name: @project.name), class: 'fa-fw h2 mb-0') %>
  </div>

  <div class="card-body">
    <div class="row">
      Iniciados e não completados no tempo correto: <%= @data[:late] %>%
    </div>

    <div class="row">
      Iniciados: <%= @data[:in_progress] %>%
    </div>

    <div class="row">
      Completos: <%= @data[:done] %>%
    </div>
  </div>
</div>
