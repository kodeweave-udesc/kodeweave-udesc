<div class="card mb-3 bg-outline-dark udesc-green-light">
  <div class="card-header h3">
    <%= icon('fas','clipboard-list', t('.title'), class: 'fa-fw h2 mb-0') %>
  </div>

  <div class="card-body">
    <div class="row">
      <%= render(@tasks) %>
    </div>
  </div>
</div>
